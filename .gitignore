# compiled binaries
*.exe
*.exe~

# log files
*.log

# runtime state (per-service status, used for recovery emails)
state.json

# output / backup folders
out/

# configuration files (contains internal hostnames, paths, recipient emails)
config.json
